section.about-vision-mission(class="relative")

    .section-content(data-default-bg="./img/1.jpg")
        .item(data-bg="./img/1.jpg")
            .item-img
            .item-content
                .item-title
                    h2.title
                        .text(class="heading-1-60 text-white font-bold uppercase")
                            | Vision
                .item-box
                    .box-content(class="flex-center flex-col gap-2 md:gap-4")
                        .item-title
                            h2.title
                                .text(class="heading-1-60 text-white font-bold uppercase")
                                    | Vision

                        .item-desc
                            p.desc
                                .text(class="body-2 text-Utility-white")
                                    | Trusted leader
                                    span(class="font-bold") in Asia's poultry industry


        .item(data-bg="./img/2.png")
            .item-img
            .item-content
                .item-title
                    h2.title
                        .text(class="heading-1-60 text-white font-bold uppercase")
                            | Mission
                .item-box
                    .box-content(class="flex-center flex-col gap-2 md:gap-4 text-center")
                        .item-title
                            h2.title
                                .text(class="heading-1-60 text-white font-bold uppercase")
                                    | Mission

                        .item-desc
                            p.desc
                                .text(class="body-2 text-Utility-white")
                                    | Trusted leader
                                    span(class="font-bold") in Asia's poultry industry
